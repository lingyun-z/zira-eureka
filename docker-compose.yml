version: "2"
services:
  zira-eureka:
    restart: always
    image: zira-eureka
    container_name: zira-eureka
    ports:
      - "2333:80"
    networks:
      - zira
networks:
  zira:
    external: true
